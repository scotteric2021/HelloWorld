#include <iostream>
using namespace std;

int main()
{
    int entry;  //Initalize an integer for input to exit
    cout << "Hello World from C++ in VS Code";
    cin >> entry;
    if (entry < 0)
    {
        break;
    }
return 0;
}